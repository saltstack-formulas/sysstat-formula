# -*- coding: utf-8 -*-
# vim: ft=yaml
sysstat:
  pkg: sysstat
  service:
    name: sysstat
    enabled: True
  config:
    path: '/etc/default/sysstat'
    enabled: "true"
    sa1_options: "-S DISK"
    sa2_options: ""
